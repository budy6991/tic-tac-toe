const gameBoard = {
    cells : [0,1,2,3,4,5,6,7,8]
}

const playerOne = (name, mark) => {
    this.name = name
    this.mark = mark 
    return {name, mark}
  };


const playerTwo = (name, mark) => {
    this.name = name
    this.mark = mark 
    return {name, mark}
  };